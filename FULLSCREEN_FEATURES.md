# 全屏分屏功能说明

## 🎯 功能概述

经过优化后的分屏扩展现在支持**自动检测当前屏幕**，并让两个浏览器窗口**完全铺满整个屏幕**，提供真正的沉浸式分屏体验。

## ✨ 新增功能

### 1. 智能屏幕检测
- **自动获取真实屏幕分辨率**：使用 `chrome.system.display.getInfo()` API 获取准确的显示器信息
- **回退机制**：如果系统API不可用，自动使用 `window.screen.availWidth/Height` 获取屏幕信息
- **多显示器支持**：自动使用主显示器的工作区域

### 2. 全屏布局优化
- **零边距布局**：窗口从屏幕边缘 (0, 0) 开始，完全铺满屏幕
- **最小间隙**：窗口之间仅保留 2px 间隙，几乎无缝
- **精确计算**：使用数学算法确保两个窗口完美分割屏幕空间

### 3. 三种分屏模式

#### 🔄 左右分屏 (Side-by-Side)
```
+-------------------+-------------------+
|                   |                   |
|    窗口1 (50%)     |    窗口2 (50%)     |
|                   |                   |
+-------------------+-------------------+
```
- 窗口1：左半屏幕，从 x=0 开始
- 窗口2：右半屏幕，从 x=屏幕宽度/2+1px 开始
- 高度：100% 屏幕高度

#### ⬆️ 上下分屏 (Top-Bottom)
```
+-----------------------------------+
|                                   |
|           窗口1 (50%)              |
|                                   |
+-----------------------------------+
|                                   |
|           窗口2 (50%)              |
|                                   |
+-----------------------------------+
```
- 窗口1：上半屏幕，从 y=0 开始
- 窗口2：下半屏幕，从 y=屏幕高度/2+1px 开始
- 宽度：100% 屏幕宽度

#### 🎯 专注模式 (Focus)
```
+-------------------+-------------------+
|                   |                   |
|                   |                   |
|    窗口1 (65%)     |    窗口2 (35%)     |
|                   |                   |
|                   |                   |
+-------------------+-------------------+
```
- 窗口1：左侧 65% 宽度，适合主要任务
- 窗口2：右侧 35% 宽度，适合辅助任务
- 高度：100% 屏幕高度

## 🚀 使用方法

### 方法1：通过扩展图标
1. 点击浏览器工具栏中的 Split View 图标
2. 选择要分屏浏览的两个网站
3. 选择分屏模式（左右/上下/专注）
4. 点击"分屏浏览"按钮

### 方法2：通过测试页面
1. 打开 `quick-test.html` 进行快速测试
2. 点击对应的分屏模式按钮
3. 系统会自动创建两个测试窗口并铺满屏幕

### 方法3：通过预设模板
扩展提供了几个预设模板：
- **工作模式**：GitHub + Google Docs
- **学习模式**：Google + Wikipedia
- **AI对话**：ChatGPT + Claude AI
- **娱乐模式**：哔哩哔哩 + 知乎

## 📋 技术实现

### 屏幕检测代码
```javascript
// 获取真实屏幕信息
async function getScreenInfo() {
  try {
    // 优先使用系统API
    const screens = await chrome.system.display.getInfo();
    if (screens && screens.length > 0) {
      const primaryScreen = screens[0];
      return {
        width: primaryScreen.workArea.width || primaryScreen.bounds.width,
        height: primaryScreen.workArea.height || primaryScreen.bounds.height
      };
    }
  } catch (error) {
    console.warn('Failed to get screen info via API, using fallback');
  }

  // 回退到标准API
  return {
    width: window.screen.availWidth || 1920,
    height: window.screen.availHeight || 1080
  };
}
```

### 布局计算代码
```javascript
// 左右分屏布局计算
case 'side-by-side':
  layout.width1 = Math.floor(screenWidth / 2) - Math.floor(windowGap / 2);
  layout.width2 = Math.floor(screenWidth / 2) - Math.floor(windowGap / 2);
  layout.left1 = 0;
  layout.left2 = layout.width1 + windowGap;
  break;
```

## 🔧 权限要求

扩展需要以下权限来实现全屏分屏功能：
- `windows`: 创建和定位浏览器窗口
- `tabs`: 获取当前标签页信息
- `storage`: 保存用户设置和偏好
- `system.display`: 获取显示器信息
- `activeTab`: 访问当前活动标签页

## 🎨 测试文件

项目包含以下测试文件：
- `quick-test.html`: 快速分屏测试页面
- `test-fullscreen.html`: 详细的全屏测试页面
- `test-screen-api.js`: 屏幕API测试脚本

## 💡 使用提示

1. **首次使用**：建议先使用 `quick-test.html` 测试功能是否正常
2. **屏幕适配**：扩展会自动适配不同分辨率和尺寸的显示器
3. **多显示器**：默认使用主显示器，可以通过拖拽窗口到其他显示器
4. **窗口管理**：可以像普通窗口一样最小化、最大化或关闭分屏窗口

## 🐛 故障排除

### 如果窗口没有铺满屏幕：
1. 检查显示器设置，确保没有缩放问题
2. 确认扩展有足够的权限创建窗口
3. 尝试刷新扩展或重新加载页面

### 如果窗口位置不正确：
1. 检查任务栏是否自动隐藏
2. 确认没有其他窗口占用屏幕空间
3. 尝试手动调整窗口位置测试

---

**享受你的全屏分屏体验！** 🎉