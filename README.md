# Split View Chrome Extension

[![Chrome Extension](https://img.shields.io/badge/Chrome-Extension-green.svg)](https://chrome.google.com/webstore)
[![License](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE)
[![Version](https://img.shields.io/badge/Version-1.0.0-orange.svg)](manifest.json)

🖥️ 一个功能强大的Chrome分屏浏览助手，提供智能窗口管理和便捷的分屏浏览体验，支持自定义预设和灵活的配置选项。

## ✨ 功能特点

### 🚀 核心功能
- **一键分屏** - 快速启动两个网页的分屏浏览，自动优化窗口布局
- **智能窗口管理** - 基于屏幕尺寸自动计算最佳窗口位置和大小
- **预设模板** - 内置AI对话、工作模式、学习组合等常用模板
- **自定义预设** - 支持用户创建、编辑和删除自定义网站组合
- **当前页面支持** - 可将当前浏览的页面直接加入分屏组合

### 🎯 用户体验
- **现代化界面** - 直观易用的弹窗界面设计
- **网站图标识别** - 自动识别并显示网站图标和名称
- **实时URL验证** - 智能验证输入的网址格式和有效性
- **设置持久化** - 自动保存用户的预设和偏好设置
- **版本显示** - 实时显示扩展版本信息

### 🔧 技术特性
- **Manifest V3** - 使用最新的Chrome Extension API规范
- **Service Worker** - 高效的后台服务脚本
- **Chrome Storage API** - 安全可靠的本地数据存储
- **跨平台兼容** - 支持Windows、macOS、Linux多平台
- **响应式设计** - 适配不同屏幕尺寸和分辨率

## 📦 安装方法

### 方法一：开发者模式安装（推荐）

1. **克隆或下载项目**
   ```bash
   git clone https://github.com/your-username/split-view-chrome-extension.git
   cd split-view-chrome-extension
   ```

2. **打开Chrome扩展管理页面**
   - 在Chrome地址栏输入 `chrome://extensions/`
   - 或通过菜单：更多工具 → 扩展程序

3. **启用开发者模式**
   - 点击页面右上角的"开发者模式"开关

4. **加载扩展**
   - 点击"加载已解压的扩展程序"
   - 选择项目文件夹 `split-view-chrome-extension`

5. **完成安装**
   - 扩展图标会出现在Chrome工具栏中

### 方法二：Chrome Web Store安装
> 暂未发布到Chrome Web Store，敬请期待

## 🚀 使用指南

### 快速开始

1. **打开扩展** - 点击Chrome工具栏中的Split View图标
2. **选择预设组合** - 从内置预设中选择或使用自定义组合
3. **一键分屏** - 点击预设右侧的"🚀 分屏"按钮

### 内置预设组合

- **🧠 DeepSeek + 🌙 Kimi** - AI对话组合，适合对比使用不同AI助手
- **💬 ChatGPT + 🤖 Claude** - 主流AI工具对比
- **💻 GitHub + 📋 Notion** - 开发工作组合
- **🔍 Google + 🌍 Wikipedia** - 搜索研究组合
- **📺 Bilibili + ❓ 知乎** - 娱乐学习组合

### 自定义预设功能

1. **创建新预设**
   - 点击页面底部的"➕ 添加新预设"按钮
   - 输入左侧和右侧网址
   - 点击"💾 保存预设"

2. **编辑预设**
   - 点击预设左上角的"✏️"编辑按钮
   - 修改网址信息
   - 点击"💾 保存更改"

3. **删除预设**
   - 编辑模式下点击预设右侧的"🗑️"删除按钮

4. **使用当前页面**
   - 点击输入框右侧的"📄 当前页面"按钮
   - 自动填入当前浏览的网页地址

### 分屏布局

扩展会自动检测屏幕尺寸并优化窗口布局：
- **自动计算** - 根据屏幕分辨率计算最佳窗口大小
- **智能定位** - 避免窗口重叠和遮挡
- **多屏支持** - 在多显示器环境下智能布局

## 🔧 配置选项

### 扩展功能
- **一键重置** - 通过重置按钮恢复默认设置
- **版本显示** - 实时显示扩展版本信息
- **自动保存** - 所有更改自动保存到本地存储

### 数据管理
扩展自动保存以下数据：
```javascript
{
  customPresets: [...],        // 用户自定义预设
  rememberLayout: true,        // 记住布局偏好
  firstTime: false,           // 首次使用标记
  lastSaved: 1699123456789    // 最后保存时间戳
}
```

### 高级特性
- **防抖保存** - 避免频繁存储操作，提升性能
- **错误恢复** - 完善的错误处理和用户提示
- **消息传递** - 前后端安全通信机制
- **权限管理** - 最小权限原则，仅请求必要权限

## 🌟 项目亮点

### 🎯 用户体验优化
- **零学习成本** - 直观的界面设计，无需说明即可上手
- **智能识别** - 自动识别网站并显示相应图标和名称
- **快速操作** - 一键分屏，无需复杂设置
- **响应迅速** - 优化的代码结构，确保快速响应

### 🛡️ 安全性保障
- **本地存储** - 所有数据仅保存在用户本地，不上传到服务器
- **权限最小化** - 仅请求必要的Chrome扩展权限
- **输入验证** - 严格的URL格式验证，防止安全问题

### 🚀 性能优化
- **异步加载** - 所有异步操作使用async/await模式
- **内存优化** - 智能的事件监听器管理，避免内存泄漏
- **代码分离** - 前后端代码分离，提升维护性和性能

## 🛠️ 开发说明

### 项目结构
```
split-view/
├── manifest.json              # 扩展配置文件 (Manifest V3)
├── popup.html                 # 扩展弹窗界面结构
├── popup.css                  # 弹窗样式文件 (12.8KB)
├── popup.js                   # 弹窗交互逻辑 (29.7KB)
├── background.js              # 后台服务脚本 (6.3KB)
├── guide.html                 # Split View功能指南 (18KB)
├── icons/                     # 扩展图标文件夹
│   ├── icon16.png             # 16x16 图标
│   ├── icon32.png             # 32x32 图标
│   ├── icon48.png             # 48x48 图标
│   └── icon128.png            # 128x128 图标
├── test*.html                 # 功能测试页面
├── manifest-store.json        # Chrome Web Store发布版本
└── docs/                      # 文档文件
    ├── CLAUDE.md              # 开发指南
    ├── DEBUG.md               # 调试说明
    ├── INSTALL.md             # 安装指南
    └── *.md                   # 其他文档
```

### 技术栈
- **前端** - HTML5, CSS3, JavaScript (ES6+)
- **API** - Chrome Extension API v3
- **存储** - Chrome Storage API
- **窗口管理** - Chrome Windows API

### 核心功能实现

#### 1. 预设管理系统
```javascript
// 自定义预设数据结构
const customPreset = {
  id: 'custom-1',              // 唯一标识符
  name: '自定义组合',           // 用户可编辑名称
  leftUrl: 'https://...',      // 左侧网站URL
  rightUrl: 'https://...',     // 右侧网站URL
  icon: '🌟',                  // 自定义图标
  createdAt: timestamp         // 创建时间
};

// 加载保存的自定义预设
async function loadCustomPresets() {
  const data = await chrome.storage.local.get(['customPresets']);
  return data.customPresets || [];
}
```

#### 2. 智能窗口布局
```javascript
// 获取屏幕信息并计算最优布局
async function getScreenInfo() {
  const displays = await chrome.system.display.getInfo();
  const primaryDisplay = displays.find(d => d.isPrimary) || displays[0];

  return {
    width: primaryDisplay.bounds.width,
    height: primaryDisplay.bounds.height,
    availWidth: primaryDisplay.workArea.width,
    availHeight: primaryDisplay.workArea.height
  };
}

// 创建分屏窗口
async function createSplitWindow(url1, url2) {
  const screenInfo = await getScreenInfo();
  const windowWidth = Math.floor(screenInfo.availWidth / 2) - 20;

  return await chrome.windows.create({
    url: url1,
    width: windowWidth,
    height: screenInfo.availHeight - 100,
    left: 0,
    top: screenInfo.availHeight > 800 ? 50 : 0,
    focused: true
  });
}
```

#### 3. 网站识别系统
```javascript
// 网站图标和名称映射
const websiteIcons = {
  'deepseek.com': '🧠',
  'claude.ai': '🤖',
  'github.com': '💻',
  'google.com': '🔍',
  // ... 更多映射
};

// 提取网站信息
function getWebsiteInfo(url) {
  try {
    const domain = new URL(url).hostname.replace('www.', '');
    return {
      icon: websiteIcons[domain] || '🌐',
      name: websiteNames[domain] || domain
    };
  } catch (error) {
    return { icon: '🌐', name: '未知网站' };
  }
}
```

#### 4. 数据持久化
```javascript
// 保存用户设置
async function saveSettings(settings) {
  await chrome.storage.local.set({
    customPresets: settings.customPresets,
    rememberLayout: settings.rememberLayout,
    lastSaved: Date.now()
  });
}

// 防抖保存避免频繁写入
const debouncedSave = debounce(saveSettings, 1000);
```

## 🔍 故障排除

### 常见问题

#### 1. 扩展无法加载
- **原因**：manifest.json格式错误或权限不足
- **解决**：检查JSON格式，确保文件路径正确

#### 2. Split View功能无法启用
- **原因**：Chrome版本过低或实验性功能已移除
- **解决**：更新Chrome到最新版本，使用替代方案

#### 3. 分屏窗口位置不正确
- **原因**：屏幕分辨率检测错误
- **解决**：手动调整窗口位置，或重启扩展

#### 4. 自定义网址无法打开
- **原因**：URL格式不正确或网站无法访问
- **解决**：检查网址格式，确保包含协议（http://或https://）

### 调试和测试

#### 开发者调试
1. **控制台日志**
   ```javascript
   // 详细的调试信息已内置在代码中
   console.log('Popup initialized successfully');
   console.log('Current tab:', currentTab?.url);
   ```

2. **Chrome DevTools调试**
   - 右键扩展图标 → "检查弹出内容" 查看前端日志
   - chrome://extensions/ → 扩展详情 → "检查视图: Service Worker" 查看后端日志

#### 测试文件
项目包含完整的测试文件套件：
- `test.html` - 基础功能测试
- `test-custom-presets.html` - 自定义预设功能测试
- `test-fullscreen.html` - 全屏功能测试
- `quick-test.html` - 快速功能验证
- `test-validation.js` - URL验证测试

#### 常见问题排查
1. **扩展无法加载**
   - 检查manifest.json语法
   - 确认文件路径正确
   - 验证权限配置

2. **窗口创建失败**
   - 检查系统显示权限
   - 验证屏幕API可用性
   - 确认URL格式正确

3. **数据保存失败**
   - 检查Chrome存储权限
   - 验证存储配额限制
   - 查看控制台错误信息

## 🤝 贡献指南

欢迎提交Issue和Pull Request！

### 开发环境设置

1. **Fork项目**
   ```bash
   git clone https://github.com/your-username/split-view-chrome-extension.git
   ```

2. **创建功能分支**
   ```bash
   git checkout -b feature/your-feature-name
   ```

3. **进行开发**
   - 修改代码
   - 测试功能
   - 更新文档

4. **提交更改**
   ```bash
   git commit -m "Add your feature description"
   git push origin feature/your-feature-name
   ```

5. **创建Pull Request**

### 代码规范
- 使用ES6+语法
- 遵循Google JavaScript代码规范
- 添加适当的注释和文档

## 📄 许可证

本项目采用 [MIT License](LICENSE) 许可证。

## 🙏 致谢

- [Chrome Extension API文档](https://developer.chrome.com/docs/extensions/)
- [Material Design图标](https://fonts.google.com/icons)
- 所有贡献者和用户的支持

## 📞 联系方式

- **项目主页**：https://github.com/your-username/split-view-chrome-extension
- **问题反馈**：https://github.com/your-username/split-view-chrome-extension/issues
- **邮箱**：your-email@example.com

---

⭐ 如果这个项目对您有帮助，请给它一个星标！

---

**免责声明**：本扩展是开源项目，仅供学习和个人使用。Chrome Split View是Google的实验性功能，可能会在未来版本中发生变化。使用前请备份重要数据。